import { BookOpen } from 'lucide-react'
import { PlaceholderPage } from '../components/ui/PlaceholderPage'

export default function Ledgers() {
  return <PlaceholderPage title="Party Ledgers / Statements" phase={5} Icon={BookOpen} />
}
